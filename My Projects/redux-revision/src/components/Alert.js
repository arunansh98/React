import { useSelector } from "react-redux";

export default function Alert(){
    // alert("Alert!")
    console.log('alert !');

    // const alertCount = useSelector((state) => state.counter.value);
    
    return <h1>Alert count !</h1>
}